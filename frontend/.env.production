# Production build: empty base URL = same-origin requests through the
# Caddy reverse proxy. This makes http://localhost and http://<LAN-IP>
# work identically on every client machine with zero configuration.
VITE_API_BASE_URL=
