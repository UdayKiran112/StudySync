import { Bar, BarChart, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { BadgeIndianRupee, CalendarDays, CircleCheck, CircleX, LibraryBig, MonitorCheck } from "lucide-react";
import type { SubscriptionSummary } from "../../api/types";
import { AnalyticsStatCard } from "../analytics/components/AnalyticsStatCard";

const colours = ["#1e2a38", "#a9782f", "#5b7c71", "#a35d4e"];

export function SubscriptionSummaryDashboard({ summary, loading }: { summary?: SubscriptionSummary; loading: boolean }) {
  return <section className="mb-6 space-y-4"><div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3"><AnalyticsStatCard className="min-h-32 p-5" to="/subscriptions" icon={LibraryBig} label="Total subscriptions" value={loading ? "…" : summary?.total ?? 0} /><AnalyticsStatCard className="min-h-32 p-5" to="/subscriptions?view=active" icon={CircleCheck} label="Active subscriptions" value={loading ? "…" : summary?.active ?? 0} /><AnalyticsStatCard className="min-h-32 p-5" to="/subscriptions?view=expired" icon={CircleX} label="Expired subscriptions" value={loading ? "…" : summary?.expired ?? 0} /><AnalyticsStatCard className="min-h-32 p-5" to="/subscriptions" icon={BadgeIndianRupee} label="Catalog cost" value={loading ? "…" : `₹${(summary?.total_cost ?? 0).toLocaleString("en-IN")}`} /><AnalyticsStatCard className="min-h-32 p-5" to="/subscriptions" icon={CalendarDays} label="Average validity" value={loading ? "…" : summary?.average_validity_days == null ? "—" : `${Math.round(summary.average_validity_days)} days`} /><AnalyticsStatCard className="min-h-32 p-5" to="/subscriptions?view=used-today" icon={MonitorCheck} label="Used today" value={loading ? "…" : summary?.used_today ?? 0} /></div><div className="grid gap-4 lg:grid-cols-2"><div className="h-64 rounded-lg border border-border bg-card p-4"><h2 className="font-display text-base font-semibold text-ink">Subscription usage</h2>{summary?.usage_by_subscription.length ? <ResponsiveContainer width="100%" height="88%"><BarChart data={summary.usage_by_subscription} layout="vertical" margin={{ left: 8 }}><XAxis type="number" allowDecimals={false} tick={{ fontSize: 11, fill: "#5b6472" }} /><YAxis type="category" dataKey="name" width={90} tick={{ fontSize: 11, fill: "#5b6472" }} /><Tooltip formatter={(value) => [value, "Sessions"]} /><Bar dataKey="count" fill="#a9782f" radius={[0, 4, 4, 0]} /></BarChart></ResponsiveContainer> : <p className="mt-8 text-sm text-slate">No subscription usage has been recorded yet.</p>}</div><div className="h-64 rounded-lg border border-border bg-card p-4"><h2 className="font-display text-base font-semibold text-ink">Subscription types</h2>{summary?.type_distribution.length ? <><ResponsiveContainer width="100%" height="80%"><PieChart><Pie data={summary.type_distribution} dataKey="count" nameKey="type" innerRadius={42} outerRadius={70} paddingAngle={2}>{summary.type_distribution.map((item, index) => <Cell key={item.type} fill={colours[index % colours.length]} />)}</Pie><Tooltip formatter={(value) => [value, "Subscriptions"]} /></PieChart></ResponsiveContainer><div className="flex flex-wrap justify-center gap-x-4 gap-y-1 text-xs text-slate">{summary.type_distribution.map((item, index) => <span key={item.type} className="flex items-center gap-1"><i className="h-2.5 w-2.5 rounded-full" style={{ backgroundColor: colours[index % colours.length] }} />{item.type} ({item.count})</span>)}</div></> : <p className="mt-8 text-sm text-slate">No type information has been added yet.</p>}</div></div></section>;
}
